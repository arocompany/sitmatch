//검색 타입
tb_search_info -> tsi_type
11:키워드
13:키워드+이미지
15:키워드+영상
17:이미지
19:영상

//SNS 타입
tb_search_result -> tsr_sns
11:구글
13:트위터
15:인스타
17:페북

//검색 상태값
11:데이터 검색중
13:데이터 검색완료
15:일치율 계산중
17:일치율 계산완료

타입별 검색 시, serpApi 호출 횟수

조건 : 국가 7개 모두 활성화, 페이징 최대 10회

keyword
    검색 * 국가 * (선택한 sns * 페이징 + youtube)
    1 * 7 * (3 * 10 + 1) = 217회 (max)
keyword + image
    검색 * 국가 * ((선택한 sns * (keyword + image) * 페이징) + (google lens + google lens image sources) )
    1 * 7 * (3 * ( 1 + 1 ) * 10 + 1 + 1 ) = 434회 (max)
keyword + video
    검색 * 국가 * 선택한 sns * 파일갯수
    1 * 7 * 3 * 12 = 21 * 12 = 파일갯수의 수량이 중요
image
    검색 * 국가 * (google_reverse_image * 페이징) + (google lens + google lens image sources))
    1 * 7 * (( 1 * 10 ) + 1 + 1 ) = 84회 (max)
video
    검색 * 국가 * 파일갯수
    1 * 7 * 12 = 파일갯수의 수량이 중요

배치
    24시간 모니터링 건 * 국가 * 페이징
    1 * 7 * 10 = 70회



`TMR_UNO,` INT(10) NOT NULL AUTO_INCREMENT,
`TSI_UNO,` INT(10) NULL DEFAULT NULL,
`TSR_UNO,` INT(10) NULL DEFAULT NULL,
`TSJ_UNO,` INT(10) NULL DEFAULT NULL COMMENT 'tb_search_job 테이블의 pk',
`TMR_V_SCORE,` DOUBLE UNSIGNED NULL DEFAULT NULL COMMENT '비디오 매칭율',
`TMR_A_SCORE,` DOUBLE UNSIGNED NULL DEFAULT NULL COMMENT '오디오 매칭율',
`TMR_T_SCORE,` DOUBLE UNSIGNED NULL DEFAULT NULL COMMENT '텍스트 매칭율',
`TMR_TOTAL_SCORE,` DOUBLE UNSIGNED NULL DEFAULT NULL COMMENT '아청물 점수',
`TMR_AGE_SCORE,` DOUBLE UNSIGNED NULL DEFAULT NULL,
`TMR_OBJECT_SCORE,` DOUBLE UNSIGNED NULL DEFAULT NULL,
`TMR_OCW_SCORE,` DOUBLE UNSIGNED NULL DEFAULT NULL,
`TMR_SIMILARITY,` DOUBLE UNSIGNED NULL DEFAULT NULL COMMENT '일치율',
`TMR_STAT,` VARCHAR(2) NULL DEFAULT NULL COMMENT '매칭 처리 상태 11:처리 성공, 13:처리 실패 ' COLLATE 'utf8mb4_general_ci',
`TMR_MESSAGE,` VARCHAR(2000) NULL DEFAULT NULL COMMENT '매칭 처리 메시지 (에러메시지등)' COLLATE 'utf8mb4_general_ci',